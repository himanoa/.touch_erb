import * as React from "react"

export type <%= file_name.camelize %>Props = {}

const <%= file_name.camelize %>: React.FC<<%= file_name.camelize %>Props> = (props: <%= file_name.camelize %>Props) => {
  return <></>
}

export default React.memo(<%= file_name.camelize %>)
